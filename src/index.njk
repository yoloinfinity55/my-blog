---
layout: base.njk
title: My Awesome Blog - Home
tags: []
---

<div class="max-w-5xl mx-auto">
    <div class="text-center py-16 bg-gradient-to-r from-teal-500 to-blue-600 text-white rounded-lg shadow-lg mb-12">
        <h1 class="text-5xl font-extrabold mb-4">Welcome to My Awesome Blog!</h1>
        <p class="text-xl mb-8">Discover interesting articles on technology, web development, and more.</p>
        <a href="#latest-posts" class="bg-white text-teal-700 hover:bg-teal-100 px-8 py-3 rounded-full text-lg font-semibold transition duration-300 ease-in-out transform hover:scale-105">Explore Posts</a>
    </div>

    <h2 id="latest-posts" class="text-3xl font-bold text-slate-900 mb-8 text-center">Latest Posts</h2>

    <div class="flex flex-wrap -mx-4">
        {%- for post in collections.posts | reverse -%}
            <div class="w-full md:w-1/2 lg:w-1/3 p-4 mb-6">
                <div class="bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300 h-full flex flex-col overflow-hidden">
                    <a href="{{ post.url | url }}" class="block">
                        {% set postImage = post.data.image or '/assets/images/default-post-image.png' %}
                        <img src="{{ postImage | url }}" alt="{{ post.data.title }} Image" class="w-full h-48 object-cover">
                    </a>
                    <div class="p-6 flex-grow flex flex-col">
                        <h3 class="font-bold text-2xl mb-3">
                            <a class="text-slate-900 hover:text-teal-600 transition-colors duration-200" href="{{ post.url | url }}">{{ post.data.title }}</a>
                        </h3>
                        <p class="text-slate-500 text-sm mb-4">
                            Published on {{ post.data.date | postDate }}
                        </p>
                        <p class="text-slate-700 text-base flex-grow mb-4">
                            {{ post.data.description }}
                        </p>
                        <a href="{{ post.url | url }}" class="inline-block mt-auto text-teal-600 hover:text-teal-800 font-semibold">Read More &rarr;</a>
                    </div>
                </div>
            </div>
        {%- endfor -%}
    </div>
</div>
